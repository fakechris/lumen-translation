{
  "name": "@lumen/userscript",
  "version": "0.1.0",
  "description": "Lumen Translation userscript (Tampermonkey/Violentmonkey) — limited fallback reusing core/engines/dom.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@lumen/core": "workspace:*",
    "@lumen/engines": "workspace:*",
    "@lumen/dom": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
